# Managed by Chef for node <%= node['fqdn'] -%>.

casa:
  engine:
    uuid: '<%= @config.uuid %>'

store:
  user_contact:
      name: '<%= @config.contact_name %>'
      email: '<%= @config.contact_email %>'
