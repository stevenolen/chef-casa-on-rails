# Managed by Chef for node <%= node['fqdn'] -%>.

caliper:
  event_store:
    url: <%= @config.caliper_url -%>
    api_key: <%= @config.caliper_api_key -%>
    sensor_id: <%= @config.caliper_sensor_id -%>
